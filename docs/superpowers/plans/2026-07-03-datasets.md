# Agent Datasets 功能实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在 `agents.yaml` 的顶层 agent 配置中新增可选的 `datasets` 字段（`Record<string, string>`），并将其格式化为 `<datasets>` 标签块追加到系统提示词中。

**Architecture:** 在 `src/config.ts` 中扩展 `AgentDefinitionSchema` 并增强 `resolveSystemPrompt()`；新增 `formatDatasets()` 辅助函数负责渲染固定格式。`src/registry.ts` 保持现状，继续通过 `resolveSystemPrompt()` 获取最终系统提示词。子 agent（`SubagentDefinitionSchema`）不加入 `datasets` 支持。

**Tech Stack:** TypeScript, Zod, Vitest, Hono, Node.js 22

## Global Constraints

- 所有本地导入必须使用 `.js` 扩展名（ESM + NodeNext）。
- 保持子 agent 不支持 `datasets`。
- 未配置 `datasets` 时行为完全不变。
- 配置 `datasets` 为空对象 `{}` 时仍需注入空的 `<datasets>` 块。
- 提交前必须运行 `npx tsc --noEmit` 和 `npm test`。
- 提交信息使用 conventional commits 前缀（如 `feat(datasets):`）。

---

## File Structure

| 文件 | 职责 |
|------|------|
| `src/config.ts` | 扩展 `AgentDefinitionSchema`，新增 `formatDatasets()`，修改 `resolveSystemPrompt()` 追加 datasets 块。 |
| `src/__tests__/config.test.ts` | 新增 datasets 解析与 `resolveSystemPrompt` 注入行为的测试。 |
| `docs/superpowers/specs/2026-07-03-datasets-design.md` | 已提交的设计文档（只读参考）。 |

---

### Task 1: 创建功能分支

**Files:**
- 无文件修改

**Interfaces:**
- 无

- [ ] **Step 1: 从 main 切出 feature 分支**

```powershell
git checkout -b feat/datasets
```

Expected: 当前分支变为 `feat/datasets`。

- [ ] **Step 2: 确认分支状态**

```powershell
git branch --show-current
```

Expected: 输出 `feat/datasets`。

---

### Task 2: 扩展配置 Schema 与系统提示词注入

**Files:**
- Modify: `src/config.ts`

**Interfaces:**
- Consumes: `AgentDefinition` 类型（新增 `datasets?: Record<string, string>`）。
- Produces: `formatDatasets(datasets: Record<string, string>): string`，`resolveSystemPrompt()` 返回的字符串在配置 datasets 时包含 `<datasets>` 块。

- [ ] **Step 1: 在 `AgentDefinitionSchema` 中新增 `datasets` 字段**

在 `src/config.ts` 的 `AgentDefinitionSchema` 中（`subagents` 字段之前）加入：

```ts
datasets: z.record(z.string()).optional(),
```

- [ ] **Step 2: 新增 `formatDatasets()` 辅助函数**

在 `resolveSystemPrompt()` 上方加入：

```ts
export function formatDatasets(datasets: Record<string, string>): string {
  const lines = Object.entries(datasets)
    .map(([id, description]) => ` - ${id}: ${description}`)
    .join("\n")
  return `<datasets>\n${lines}\n</datasets>`
}
```

- [ ] **Step 3: 修改 `resolveSystemPrompt()` 追加 datasets**

将现有函数替换为：

```ts
export function resolveSystemPrompt(agent: AgentDefinition, configDir: string): string | undefined {
  let base: string | undefined
  if (agent.systemPrompt) {
    base = agent.systemPrompt
  } else if (agent.systemPromptFile) {
    const filePath = resolve(configDir, agent.systemPromptFile)
    base = readFileSync(filePath, "utf-8")
  }

  if (agent.datasets) {
    const datasetsBlock = formatDatasets(agent.datasets)
    return base ? `${base}\n\n${datasetsBlock}` : datasetsBlock
  }

  return base
}
```

- [ ] **Step 4: 提交 Schema 与注入逻辑**

```powershell
git add src/config.ts
git commit -m "feat(datasets): add datasets field and inject into system prompt"
```

---

### Task 3: 编写测试

**Files:**
- Modify: `src/__tests__/config.test.ts`

**Interfaces:**
- Consumes: `RuntimeConfigSchema.parse()`、`resolveSystemPrompt()`、`formatDatasets()`（从 `src/config.js` 导入）。
- Produces: 5 个新测试用例，覆盖解析、追加、无 systemPrompt、空 datasets、无 datasets。

- [ ] **Step 1: 导入 `formatDatasets`**

在 `src/__tests__/config.test.ts` 的 import 块中加入：

```ts
formatDatasets,
```

- [ ] **Step 2: 在 `RuntimeConfigSchema` describe 中新增解析测试**

在 `accepts a valid full config with all fields` 测试之后，或 describe 末尾加入：

```ts
it("accepts agent with datasets", () => {
  const config = {
    agents: [
      {
        id: "dataset-agent",
        datasets: {
          "dataset-1": "Primary dataset",
          "dataset-2": "Secondary dataset",
        },
      },
    ],
  }
  const result = RuntimeConfigSchema.parse(config)
  expect(result.agents[0].datasets).toEqual({
    "dataset-1": "Primary dataset",
    "dataset-2": "Secondary dataset",
  })
})
```

- [ ] **Step 3: 在 `resolveSystemPrompt` describe 中新增注入测试**

在 `resolveSystemPrompt` describe 末尾加入：

```ts
it("appends formatted datasets to systemPrompt", () => {
  const agent: AgentDefinition = {
    id: "a",
    model: "claude-sonnet-4-6",
    maxTurns: 10,
    systemPrompt: "Be helpful.",
    datasets: {
      "dataset-1": "Primary dataset",
      "dataset-2": "Secondary dataset",
    },
  }
  expect(resolveSystemPrompt(agent, "/tmp")).toBe(
    "Be helpful.\n\n<datasets>\n - dataset-1: Primary dataset\n - dataset-2: Secondary dataset\n</datasets>",
  )
})

it("returns only datasets block when systemPrompt is not set", () => {
  const agent: AgentDefinition = {
    id: "b",
    model: "claude-sonnet-4-6",
    maxTurns: 10,
    datasets: {
      "dataset-1": "Primary dataset",
    },
  }
  expect(resolveSystemPrompt(agent, "/tmp")).toBe(
    "<datasets>\n - dataset-1: Primary dataset\n</datasets>",
  )
})

it("returns empty datasets block when datasets is empty object", () => {
  const agent: AgentDefinition = {
    id: "c",
    model: "claude-sonnet-4-6",
    maxTurns: 10,
    datasets: {},
  }
  expect(resolveSystemPrompt(agent, "/tmp")).toBe("<datasets>\n\n</datasets>")
})

it("returns unchanged systemPrompt when datasets is not configured", () => {
  const agent: AgentDefinition = {
    id: "d",
    model: "claude-sonnet-4-6",
    maxTurns: 10,
    systemPrompt: "Be helpful.",
  }
  expect(resolveSystemPrompt(agent, "/tmp")).toBe("Be helpful.")
})
```

- [ ] **Step 4: 新增 `formatDatasets` 单元测试**

在文件末尾新增 describe：

```ts
describe("formatDatasets", () => {
  it("formats multiple datasets", () => {
    expect(
      formatDatasets({
        "dataset-1": "Primary dataset",
        "dataset-2": "Secondary dataset",
      }),
    ).toBe("<datasets>\n - dataset-1: Primary dataset\n - dataset-2: Secondary dataset\n</datasets>")
  })

  it("formats empty datasets", () => {
    expect(formatDatasets({})).toBe("<datasets>\n\n</datasets>")
  })
})
```

- [ ] **Step 5: 提交测试**

```powershell
git add src/__tests__/config.test.ts
git commit -m "test(datasets): add system prompt injection tests"
```

---

### Task 4: 验证与最终提交

**Files:**
- 无新增文件

**Interfaces:**
- 无

- [ ] **Step 1: 类型检查**

```powershell
npx tsc --noEmit
```

Expected: 无错误，退出码 0。

- [ ] **Step 2: 运行测试**

```powershell
npm test
```

Expected: 所有测试通过（当前基线 95 tests passed）。

- [ ] **Step 3: 单独运行 config 测试确认新增用例**

```powershell
npx vitest run src/__tests__/config.test.ts
```

Expected: 新增 5 个测试 + 原有测试全部通过。

- [ ] **Step 4: 查看变更摘要**

```powershell
git log --oneline feat/datasets --not main
git diff --stat main
```

Expected: 分支有 2 个提交（config + tests），修改 `src/config.ts` 和 `src/__tests__/config.test.ts`。

- [ ] **Step 5: 最终确认无需额外提交**

此时 feature 分支已完成，等待用户决定是否推送或创建 PR。

---

## Self-Review

**1. Spec coverage:**
- ✅ `AgentDefinitionSchema` 新增 `datasets` 字段 → Task 2 Step 1。
- ✅ `resolveSystemPrompt()` 追加 datasets 块 → Task 2 Step 3。
- ✅ 无 `systemPrompt` 时返回纯 datasets 块 → Task 3 Step 3 测试 2。
- ✅ 空对象 `{}` 仍注入空 `<datasets>` 块 → Task 3 Step 3 测试 3。
- ✅ 未配置 `datasets` 时行为不变 → Task 3 Step 3 测试 4。
- ✅ 子 agent 不支持 `datasets` → 计划中未修改 `SubagentDefinitionSchema`。

**2. Placeholder scan:**
- 无 TBD/TODO/"implement later"。
- 每个步骤都包含实际代码或命令。

**3. Type consistency:**
- `datasets` 类型为 `Record<string, string>`，与 `z.record(z.string())` 一致。
- `formatDatasets` 接收 `Record<string, string>`，返回 `string`。
- `resolveSystemPrompt` 返回类型保持 `string | undefined`。

---

## Execution Handoff

Plan complete and saved to `docs/superpowers/plans/2026-07-03-datasets.md`. Two execution options:

**1. Subagent-Driven (recommended)** - I dispatch a fresh subagent per task, review between tasks, fast iteration

**2. Inline Execution** - Execute tasks in this session using executing-plans, batch execution with checkpoints

Which approach?
